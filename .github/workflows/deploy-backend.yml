name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  PYTHON_VERSION: "3.12"

jobs:
  test:
    name: Lint & Test
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install ruff pytest pytest-cov

      - name: Lint
        run: ruff check lambdas/

      - name: Type check imports
        run: |
          python - <<'EOF'
          import pathlib, sys
          errors = []
          for f in pathlib.Path('lambdas').rglob('*.py'):
              if f.name == '__init__.py':
                  continue
              try:
                  compile(f.read_text(), str(f), 'exec')
              except SyntaxError as e:
                  errors.append(f'{f}: {e}')
          if errors:
              print('\n'.join(errors))
              sys.exit(1)
          print('All files pass syntax check')
          EOF

  build-and-deploy:
    name: Build & Deploy Lambdas
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Build Lambda packages
        run: |
          chmod +x build.sh
          ./build.sh

      - name: Build dependency layer
        run: |
          mkdir -p build/layer/python
          # Install only runtime deps (no dev tools) and strip bytecode to keep size down
          pip install -r requirements.txt \
            -t build/layer/python \
            --quiet \
            --no-compile
          # Remove test dirs, dist-info, and __pycache__ to trim the zip
          find build/layer/python -type d -name "tests" -exec rm -rf {} + 2>/dev/null || true
          find build/layer/python -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true
          find build/layer/python -name "*.dist-info" -exec rm -rf {} + 2>/dev/null || true
          cd build/layer && zip -r ../dependencies-layer.zip python/ -x "*.pyc"
          echo "Layer zip size: $(du -sh ../dependencies-layer.zip | cut -f1)"

      - name: Publish Lambda layer via S3
        id: layer
        run: |
          # Lambda direct upload cap is 70 MB — use S3 to avoid RequestEntityTooLargeException
          S3_KEY="lambda-layers/scout-dependencies-$(date +%s).zip"
          aws s3 cp build/dependencies-layer.zip \
            s3://${{ secrets.S3_BUCKET_NAME }}/${S3_KEY} \
            --no-progress

          LAYER_ARN=$(aws lambda publish-layer-version \
            --layer-name scout-dependencies \
            --content S3Bucket=${{ secrets.S3_BUCKET_NAME }},S3Key=${S3_KEY} \
            --compatible-runtimes python3.12 \
            --query 'LayerVersionArn' \
            --output text)

          # Clean up the layer zip from S3 — it's only needed during publish
          aws s3 rm s3://${{ secrets.S3_BUCKET_NAME }}/${S3_KEY}

          echo "layer_arn=$LAYER_ARN" >> "$GITHUB_OUTPUT"
          echo "Published layer: $LAYER_ARN"

      - name: Deploy crawler Lambdas
        run: |
          FUNCTIONS=(
            "scout-crawler-linkedin"
            "scout-crawler-indeed"
            "scout-crawler-glassdoor"
            "scout-crawler-ziprecruiter"
            "scout-crawler-dice"
          )
          for fn in "${FUNCTIONS[@]}"; do
            echo "Deploying $fn..."
            aws lambda update-function-code \
              --function-name "$fn" \
              --zip-file fileb://build/crawlers.zip \
              --publish > /dev/null
            aws lambda update-function-configuration \
              --function-name "$fn" \
              --layers "${{ steps.layer.outputs.layer_arn }}" > /dev/null
            echo "  ✓ $fn deployed"
          done

      - name: Deploy enrichment Lambda
        run: |
          aws lambda update-function-code \
            --function-name scout-enrichment \
            --zip-file fileb://build/enrichment.zip \
            --publish > /dev/null
          aws lambda update-function-configuration \
            --function-name scout-enrichment \
            --layers "${{ steps.layer.outputs.layer_arn }}" > /dev/null
          echo "✓ scout-enrichment deployed"

      - name: Deploy API Lambdas
        run: |
          FUNCTIONS=(
            "scout-api-get-jobs"
            "scout-api-update-status"
            "scout-api-user-settings"
          )
          for fn in "${FUNCTIONS[@]}"; do
            echo "Deploying $fn..."
            aws lambda update-function-code \
              --function-name "$fn" \
              --zip-file fileb://build/api.zip \
              --publish > /dev/null
            aws lambda update-function-configuration \
              --function-name "$fn" \
              --layers "${{ steps.layer.outputs.layer_arn }}" > /dev/null
            echo "  ✓ $fn deployed"
          done

      - name: Deploy report Lambdas
        run: |
          FUNCTIONS=(
            "scout-daily-report"
            "scout-weekly-report"
          )
          for fn in "${FUNCTIONS[@]}"; do
            echo "Deploying $fn..."
            aws lambda update-function-code \
              --function-name "$fn" \
              --zip-file fileb://build/reports.zip \
              --publish > /dev/null
            aws lambda update-function-configuration \
              --function-name "$fn" \
              --layers "${{ steps.layer.outputs.layer_arn }}" > /dev/null
            echo "  ✓ $fn deployed"
          done

      - name: Deploy purge Lambda
        run: |
          aws lambda update-function-code \
            --function-name scout-purge \
            --zip-file fileb://build/crawlers.zip \
            --publish > /dev/null
          echo "✓ scout-purge deployed"

      - name: Summary
        run: |
          echo "## Backend Deployment Complete" >> "$GITHUB_STEP_SUMMARY"
          echo "- Layer: \`${{ steps.layer.outputs.layer_arn }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Packages: crawlers, enrichment, api, reports" >> "$GITHUB_STEP_SUMMARY"
